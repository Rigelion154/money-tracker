import { observer } from 'mobx-react-lite';

import ExpensesContent from '../../components/expenses/ExpensesContent.tsx';

const ExpensesPage = observer(() => <ExpensesContent />);

export default ExpensesPage;
